<div data-context="general--nav">
        <nav class="class_movile">
           <%= image_tag 'exit_image.png', :id => "exit-menu-movile" %>
            <div class="ABC_movile"><p>A&B</p></div>
             <div class="container_link_movile_nav">
                <%= link_to 'Perfil', perfil_path(User.find_by(id: current_user.id).perfil.id),:class=>"link_movile_nav" %>
             </div>

             <div class="container_link_movile_nav">
                <%= link_to 'Imagenes', perfil_galeries_path(User.find_by(id: current_user.id).perfil.id), :class=>"link_movile_nav"%> 
             </div>
             <div class="container_link_movile_nav">
                <%= link_to 'Cuentas', perfil_bank_accounts_path(User.find_by(id: current_user.id).perfil.id), :class=>"link_movile_nav" %>
             </div>
            <div class="container_link_movile_nav"><%= link_to "Cerrar sesión", destroy_user_session_path, method: :delete, :class=>"link_movile_nav"%></div>
        </nav>

  
   <nav  class="navCreateUsuario">
        <%= image_tag 'Header.png', :class => "burguer", :id=>'menu-burger'%>
        <div class="logoAB-profile">
            <p class="p-logoAb">A&B</p>
        </div>
        <div class="profile">
<!--image_tag(User.find_by(id: params[:perfil_id]).perfil.image
User.find_by(id: params[:perfil_id]).perfil
-->
        <% if Perfil.find_by(id:params[:perfil_id]).image.attached? %>
           <%=Perfil.find_by(id:params[:perfil_id]).image, :class=>"image-profile")%> 
         <%else%>
            <%=image_tag('user_picture.jpeg', :class=>"image-profile" )%>
        <% end %>

            <h1 class="name-user"><%=Perfil.find_by(id:params[:perfil_id]).full_name%></h1>
        </div>
    </nav>
</div>