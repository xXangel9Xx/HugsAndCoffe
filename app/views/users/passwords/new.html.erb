<div class="container-forgot-password-body">
    <nav class="nav-HugsAndCoffe">
        <div class="logoAB">
            <p class="p-logoAb">A&B</p>
        </div>
    </nav>
  <div class="container-forgot-password">
    <h2 class="forgot-your-password-text">olvidaste tu contraseña?</h2>
      <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
          <%= render "devise/shared/error_messages", resource: resource %>

          <div class="field-forgot-password">
            <%= f.label :email %><br />
            <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
          </div>

          <div class="actions-forgot-password">
            <%= f.submit "Send me reset password instructions" %>
          </div>
      <% end %>
      <div class="links-redirect-forgot">
    <%= render "users/shared/links" %>
      </div>
  </div>
</div>